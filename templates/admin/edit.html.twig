{% extends 'base.html.twig' %}

{% block title %}Edit employé{% endblock %}

{% block body %}

    {{ include('composants/header.html.twig') }}
    <section class="ml-64 mt-32">
        {% include 'composants/_adminSideBar.html.twig' with {'current_page': 'admin'} %}

        <section class='dark:text-primary'>
            <div class='m-8'>
                <h1 class="text-6xl text-black dark:text-third pb-8 font-light">Modification fiche employé</h1>
            </div>
            {% include('composants/_customForm.html.twig')
                with {
                'form': form,
                'updateAction': 'Mettre à jour',
                'backAction': 'app_admin_index'
            }
            %}
        </section>
    </section>

    {#    {{ include('client/_delete_form.html.twig') }}#}
{% endblock %}